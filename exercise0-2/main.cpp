#include <iostream>

int main() {
    // Note that printing this string requires escaping the " character and the \ character
    std::cout << "This (\") is a quote, and this (\\) is a backslash." << std::endl;
    return 0;
}
