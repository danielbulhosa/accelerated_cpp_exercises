#include <iostream>

int main() {
    // Note that leading tabs on a line are quite big but subsequent tabs are much smaller. Interesting.
    std::cout << "\tHere I am testing a tab\tHere is a second tab\t\t\tHere are three more tabs." << std::endl;
    return 0;
}
